[pytest]
pythonpath = src
addopts = -p no:cacheprovider
norecursedirs = .git .pytest_cache .pytest-tmp pytest-cache-files-*
markers =
    slow: marks tests that are useful smoke checks but slower than the default suite
